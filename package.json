{
  "name": "dotenv",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "gh:grepTags": "npx cypress run --env grepTags='banana',grepFilterSpecs=true --record --parallel",
    "azure:grepTags": "npx cypress run --env grepTags='banana',grepFilterSpecs=true"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cypress/grep": "^3.1.3",
    "cypress": "^12.5.0"
  }
}
